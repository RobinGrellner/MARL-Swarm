{
  "metadata": {
    "experiment_name": "embedding_dimension_scaling_pursuit_evasion",
    "version": "3.1",
    "description": "Experiment 1 (Pursuit-Evasion variant): Core thesis - Embedding Dimension x Training Swarm Size matrix on PE task.",
    "thesis_chapter": "4.2 - Dimensional Reduction via Mean Embeddings (Task 2)",
    "task": "Pursuit-Evasion",
    "created": "2026-01-24",
    "estimated_total_runs": 70,
    "estimated_runtime_hours": 233,
    "optimization_notes": "2 seeds, 500 iterations (n_steps=500, batch_size=128, n_envs=2). max_steps=1000, world_size=100, v_max=5.0, evader_speed=10.0, capture_radius=1.0 (harder). Auto-expanded from matrix_parameters."
  },

  "defaults": {
    "env_config": {
      "world_size": 100.0,
      "max_steps": 1000,
      "obs_model": "global_basic",
      "evader_strategy": "huttenrauch",
      "capture_radius": 1.0,
      "evader_speed": 10.0,
      "v_max": 5.0,
      "omega_max": 2.0,
      "kinematics": "single",
      "max_pursuers": 100,
      "torus": true
    },
    "train_config": {
      "activation": "relu",
      "aggregation": "mean",
      "phi_layers": 1,
      "algorithm": "trpo",
      "n_steps": 500,
      "batch_size": 128,
      "n_iterations": 500,
      "learning_rate": 0.001,
      "verbose": 1
    }
  },

  "matrix_parameters": {
    "description": "Cartesian product: 5 training sizes x 7 embedding dimensions x 2 seeds = 70 unique configs. Auto-expanded via config_utils.py.",
    "num_pursuers": [4, 16, 32, 50, 100],
    "embed_dim": [4, 8, 16, 32, 64, 128, 256],
    "seed": [0, 123]
  },

  "experiments": {
    "_example_num_pursuers4_embed_dim4_seed0": {
      "description": "Example: 4 pursuers, embed_dim=4, seed=0. Policy width auto-scales with embed_dim.",
      "env_config": { "num_pursuers": 4 },
      "train_config": {
        "embed_dim": 4,
        "seed": 0
      }
    },
    "_example_num_pursuers16_embed_dim64_seed123": {
      "description": "Example: 16 pursuers, embed_dim=64, seed=123. Policy width auto-scales with embed_dim.",
      "env_config": { "num_pursuers": 16 },
      "train_config": {
        "embed_dim": 64,
        "seed": 123
      }
    },
    "_abbreviated": "Full 70 configs auto-generated from matrix_parameters by config_utils.py. Naming: num_pursuers{N}_embed_dim{D}_seed{S}"
  }
}
