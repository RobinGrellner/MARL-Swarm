{
  "metadata": {
    "experiment_name": "architecture_effects_pursuit_evasion",
    "version": "2.1",
    "description": "Experiment 2 (Pursuit-Evasion variant): Architecture Effects on PE task. With embedding_dim fixed at optimal value (64), test if activation/depth/width matter.",
    "thesis_chapter": "4.3 - Architecture Simplicity with Mean Embeddings (Task 2)",
    "task": "Pursuit-Evasion",
    "created": "2026-01-24",
    "estimated_total_runs": 81,
    "estimated_runtime_hours": 405,
    "note": "Parallel structure to architecture_scalability.json but for PE task."
  },

  "defaults": {
    "env_config": {
      "num_pursuers": 16,
      "world_size": 10.0,
      "obs_model": "global_basic",
      "evader_strategy": "huttenrauch",
      "capture_radius": 0.5,
      "evader_speed": 1.0,
      "v_max": 1.0,
      "omega_max": 1.0,
      "kinematics": "single",
      "max_pursuers": 200,
      "torus": true
    },
    "train_config": {
      "embed_dim": 64,
      "aggregation": "mean",
      "algorithm": "trpo",
      "total_timesteps": 5000000,
      "learning_rate": 0.001,
      "verbose": 1
    }
  },

  "matrix_parameters": {
    "description": "Cartesian product: 3 activations x 3 depths x 3 widths = 27 unique configs. Each run 3 times with different seeds.",
    "activations": ["relu", "tanh", "gelu"],
    "depths": [1, 2, 4],
    "widths": [[32, 32], [64, 64], [128, 128]],
    "seeds": [0, 123, 456]
  },

  "experiments": {
    "pe_arch_relu_d1_w32_seed0": {
      "description": "relu activation, depth=1, width=[32,32], seed=0",
      "train_config": {
        "activation": "relu",
        "phi_layers": 1,
        "policy_layers": [32, 32],
        "seed": 0
      }
    },
    "pe_arch_tanh_d2_w64_seed0": {
      "description": "tanh activation, depth=2, width=[64,64], seed=0",
      "train_config": {
        "activation": "tanh",
        "phi_layers": 2,
        "policy_layers": [64, 64],
        "seed": 0
      }
    },
    "_abbreviated": "Full config mirrors architecture_scalability.json with 3 activations × 3 depths × 3 widths × 3 seeds = 81 configurations. Naming: pe_arch_{activation}_d{depth}_w{width[0]}_seed{seed}."
  },

  "generation_note": "Python script: For each (activation, depth, width) triplet, generate 3 configs with seeds [0, 123, 456]."
}
